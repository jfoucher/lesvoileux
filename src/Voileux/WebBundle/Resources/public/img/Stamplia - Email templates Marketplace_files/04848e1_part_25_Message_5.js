// Generated by CoffeeScript 1.4.0
(function() {

  App.Model.Message = App.Model.extend({
    namespace: 'message',
    url: function() {
      if (this.get('id')) {
        return "" + App.urlRoot + "/users/" + App.user.id + "/messages/" + (this.get('id'));
      } else {
        return "" + App.urlRoot + "/users/" + App.user.id + "/messages";
      }
    }
  });

  App.Model.MessageCollection = App.Collection.extend({
    model: App.Model.Message,
    namespace: "messages",
    url: function() {
      if (this.received) {
        return ("" + App.urlRoot + "/users/" + App.user.id + "/messages?user=") + App.user.id;
      } else {
        return "" + App.urlRoot + "/users/" + App.user.id + "/messages";
      }
    }
  });

}).call(this);
