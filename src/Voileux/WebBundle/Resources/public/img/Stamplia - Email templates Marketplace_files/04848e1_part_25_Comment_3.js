// Generated by CoffeeScript 1.4.0
(function() {

  App.Model.Comment = App.Model.extend({
    namespace: 'comment',
    url: function() {
      if (this.get('id')) {
        return "" + App.urlRoot + "/users/" + App.user.id + "/comments/" + (this.get('id'));
      } else {
        return "" + App.urlRoot + "/users/" + App.user.id + "/comments";
      }
    }
  });

  App.Model.CommentCollection = App.Collection.extend({
    model: App.Model.Comment,
    namespace: "comments",
    url: function() {
      return "" + App.urlRoot + "/users/" + App.user.id + "/comments";
    }
  });

}).call(this);
