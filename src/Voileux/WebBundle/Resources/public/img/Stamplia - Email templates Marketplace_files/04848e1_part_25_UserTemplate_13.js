// Generated by CoffeeScript 1.4.0
(function() {

  App.Model.UserTemplate = App.Model.extend({
    namespace: "template",
    defaults: {
      id: null,
      name: ''
    },
    url: function() {
      if (this.get('id')) {
        return "" + App.urlRoot + "/users/" + App.user.id + "/templates/" + (this.get('id'));
      } else {
        return "" + App.urlRoot + "/users/" + App.user.id + "/templates";
      }
    }
  });

  App.Model.UserTemplateCollection = App.Collection.extend({
    model: App.Model.UserTemplate,
    namespace: "templates",
    fetched: null,
    category: null,
    paginator_core: {
      type: 'GET',
      dataType: 'json',
      url: function() {
        return "" + App.urlRoot + "/users/" + App.user.id + "/templates";
      }
    },
    paginator_ui: {
      firstPage: 1,
      currentPage: 1,
      perPage: App.perPage,
      totalPages: 10
    },
    server_api: function() {
      var ret;
      ret = {
        'per_page': function() {
          return this.perPage;
        },
        'page': function() {
          return this.currentPage;
        },
        'orderby': 'id',
        'format': 'json'
      };
      return ret;
    },
    url: function() {
      if (this.category) {
        return "" + App.urlRoot + "/categories/" + (this.category.get('name')) + "/templates";
      } else {
        return "" + App.urlRoot + "/users/" + App.user.id + "/templates";
      }
    }
  });

}).call(this);
